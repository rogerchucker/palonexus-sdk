[tool.uv.workspace]
members = ["python"]

[dependency-groups]
dev = [
  "hatch-vcs>=0.4",
  "hatchling>=1.27",
  "jsonschema>=4.25",
  "pytest>=8.4",
  "ruff>=0.12",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["foundation_tests", "protocol/tests", "python/tests"]
