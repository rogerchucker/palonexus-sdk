[tool.uv.workspace]
members = ["python"]

[dependency-groups]
dev = [
  "hatch-vcs>=0.4",
  "hatchling>=1.27",
  "pytest>=8.4",
  "ruff>=0.12",
]

[tool.pytest.ini_options]
testpaths = ["foundation_tests", "python/tests"]
